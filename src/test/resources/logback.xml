<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Appender to print to console -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Appender to write to Allure results -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>target/allure-results/logs.log</file>
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Root logger configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Set logging level for specific packages -->
    <logger name="com.swaglabs" level="DEBUG" additivity="true"/>
    <logger name="org.openqa.selenium" level="INFO"/>

</configuration>
